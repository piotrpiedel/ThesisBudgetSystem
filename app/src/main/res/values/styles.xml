<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:textViewStyle">@style/MyTextViewStyle</item>
        <item name="editTextStyle">@style/MyEditTextStyle</item>

    </style>

    <style name="MyTextViewStyle" parent="android:Widget.TextView">
        <item name="android:textSize">@dimen/global_text_view_text_size</item>
        <item name="android:padding">@dimen/global_padding</item>
    </style>

    <style name="MyEditTextStyle" parent="android:Widget.EditText">
        <item name="android:textSize">@dimen/global_text_size_input</item>
        <item name="android:inputType">textCapSentences</item>
        <item name="android:padding">@dimen/global_padding</item>
    </style>

    <style name="TextInputLayoutHint" parent="TextAppearance.Design.Hint">
        <item name="android:textSize">16sp</item>
    </style>

    <style name="AppTheme.ToolbarStyle">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="android:textColorSecondary">@android:color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ButtonStyle">
        <item name="colorControlHighlight">@color/accent</item>
        <item name="colorButtonNormal">@color/primary</item>
    </style>

    <style name="OperationInputStyle" parent="android:style/Widget.Holo.Spinner">
        <item name="android:layout_alignParentStart">true</item>
        <item name="android:gravity">center</item>
    </style>

</resources>
